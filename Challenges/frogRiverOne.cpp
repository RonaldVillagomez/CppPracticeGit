#include <iostream>
#include <string>
#include <algorithm>
#include <bitset>
#include <vector>
#include <unordered_set>
using namespace std;

// Hashsets are useful in situations where you need to quickly check whether
// an item is present in a collection, without requiring any specific ordering
// of the elements.

int frogRiverOne(constexpr X, vector<int> &A){
    unordered_set<int> positions;
    
    cout << isLeafHere << endl;
    return 0;
}

int main(){
    vector<int> A = {1, 3, 4, 3, 5, 2, 5, 2, 1};
    const int X = 4;
    cout << "The time it takes until it is possible for the frog to jump across is: " << endl;
    cout << frogRiverOne(X, A);
}